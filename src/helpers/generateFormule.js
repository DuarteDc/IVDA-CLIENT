export const generateFormule = (dependency = '', administartiveUnit = '', section = '', serie = '') => {

    let dependencyName = dependency.split(' ');
    if (dependency.length > 1) {
        const firstWord = dependency[1];
        const lastWord = dependency[dependency.length - 1]

    }



}